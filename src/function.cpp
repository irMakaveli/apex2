#include <iostream>
#include "function.hpp"
#include <vector>
using namespace std;
int * FindUserIndex(string un,string ip,Bank bank)
{
    int index[2]={1,1};
  //  for(int i = 0 ; i < bank.usersize(); i++)
    {
       
    }
}